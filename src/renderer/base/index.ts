/**
 * @group Renderer
 * @summary Base renderer abstractions
 *
 * Common abstractions and interfaces that all renderer implementations
 * (Canvas, SVG, etc.) must conform to.
 */

export * from "./BaseRenderer"
