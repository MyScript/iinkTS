
/**
 * @group History
 */
export type THistoryConfiguration = {
  maxStackSize: number
}

/**
 * @group History
 * @source
 */
export const DefaultHistoryConfiguration: THistoryConfiguration = {
  maxStackSize: 100
}
